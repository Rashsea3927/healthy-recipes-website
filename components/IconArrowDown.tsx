import IconChevronDown from '@assets/icon-chevron-down.svg';
import Image from 'next/image';

const IconArrowDown = () => {
  return <Image src={IconChevronDown} width={20} height={21} alt='Arrow Down' />;
};

export default IconArrowDown;
